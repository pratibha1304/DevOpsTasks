# Start with the official Red Hat Universal Base Image 9 that is pre-configured for systemd.
# This image's default CMD is already set to "/sbin/init".
FROM registry.access.redhat.com/ubi9/ubi-init

# You can add other packages here if needed. For example:
# RUN microdnf install -y httpd

# EXPOSE 80

# Do not add a CMD or ENTRYPOINT line unless you have a specific reason,
# as it might override the image's default and cause errors.
